{
  "name": "root",
  "version": "0.0.0",
  "packageManager": "bun@1.0.22",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "dev:web": "turbo --filter=web dev",
    "dev:ui": "turbo --filter=flowbite-react dev",
    "dev:storybook": "turbo --filter=storybook dev",
    "build": "turbo build",
    "build:web": "turbo --filter=web build",
    "build:ui": "turbo --filter=flowbite-react build",
    "build:storybook": "turbo --filter=storybook build",
    "lint": "turbo lint",
    "lint:check": "turbo lint:check",
    "format": "turbo format",
    "format:check": "turbo format:check",
    "check": "turbo check",
    "typecheck": "turbo typecheck",
    "clean": "turbo clean && rm -rf .turbo node_modules"
  },
  "devDependencies": {
    "@biomejs/biome": "1.5.2",
    "@happy-dom/global-registrator": "13.1.4",
    "@types/bun": "1.0.1",
    "@types/web": "0.0.134",
    "turbo": "latest"
  },
  "workspaces": ["apps/*", "packages/*"]
}
