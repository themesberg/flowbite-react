import type { FlowbiteBlockquoteTheme } from './Blockquote';

export const blockquoteTheme: FlowbiteBlockquoteTheme = {
  root: {
    base: 'text-xl italic font-semibold text-gray-900 dark:text-white',
  },
};
